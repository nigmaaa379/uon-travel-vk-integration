import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';test('sales accents are editable and customer text changes are present',async()=>{const accents=JSON.parse(await readFile(new URL('../web/data/sales-accents.json',import.meta.url),'utf8'));assert.equal(accents.length,3);for(const item of accents){assert.equal(typeof item.label,'string');assert.equal(typeof item.title,'string');assert.equal(typeof item.text,'string');assert.equal(typeof item.button,'string')}const app=await readFile(new URL('../web/assets/app.js',import.meta.url),'utf8');assert.match(app,/Забота 24\/7/);assert.match(app,/Трансферы, визы, досуг/);assert.match(app,/в тропической стране/i)})

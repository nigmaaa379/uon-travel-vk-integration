import test from 'node:test';import assert from 'node:assert/strict';import { MaxClientV2 } from '../src/max-client-v2.js';
test('MAX bot_started becomes /start',()=>{const client=new MaxClientV2({token:'token',webhookSecret:'secret',apiUrl:'https://platform-api2.max.ru'});const parsed=client.parse({update_type:'bot_started',timestamp:10,user:{user_id:42},chat_id:99});assert.equal(parsed.userId,'42');assert.deepEqual(parsed.input,{text:'/start'})});
test('MAX startup registers bot_started webhook',async()=>{const original=globalThis.fetch;let request;globalThis.fetch=async(url,options)=>{request={url,options};return new Response(JSON.stringify({success:true}),{status:200,headers:{'content-type':'application/json'}})};try{const client=new MaxClientV2({token:'token',webhookSecret:'secret',apiUrl:'https://platform-api2.max.ru'});await client.configure();const body=JSON.parse(request.options.body);assert.equal(request.options.method,'POST');assert.ok(body.update_types.includes('bot_started'));assert.equal(body.url,'https://tursbezhimnamore.ru/max/webhook')}finally{globalThis.fetch=original}});

const toNumber = (value) => Number(String(value ?? '').replace(/[^0-9.]/g, '')) || 0;
export class TourvisorClient { constructor(config) { this.config = config; } async search(params) { const response = await fetch(this.config.searchEndpoint, { method: this.config.method, headers: { Authorization: this.config.token, 'content-type': 'application/json' }, body: this.config.method === 'GET' ? undefined : JSON.stringify(params), signal: AbortSignal.timeout(30000) }); if (!response.ok) throw new Error(`Tourvisor HTTP ${response.status}`); const data = await response.json(); const rows = data.tours || data.hotTours || data.result?.tours || data.data?.tours || []; return rows.map((t) => this.#normalize(t, params)).filter((t) => t.id && t.price).sort((a, b) => a.price - b.price).slice(0, this.config.limit); } #normalize(t, params) { const id = String(t.id ?? t.tourid ?? t.offer_id ?? ''); const destination = t.destination ?? t.countryname ?? t.country ?? params.destination; const dates = t.dates ?? t.flydate ?? t.date ?? params.dates; const price = toNumber(t.price ?? t.tourprice ?? t.amount); const hotel = t.hotel ?? t.hotelname ?? t.name ?? 'Тур'; const site = new URL(this.config.siteSearchUrl); site.searchParams.set('tourId', id); site.searchParams.set('destination', destination || ''); site.searchParams.set('dates', dates || ''); site.searchParams.set('group', params.group || ''); return { id, destination, dates, price, hotel, group: params.group, siteUrl: site.toString() }; } }
export class HotToursScheduler { constructor({ store, tourvisor, clients, hours, logger = console }) { Object.assign(this, { store, tourvisor, clients, hours, logger }); this.lastRun = null; } start() { this.timer = setInterval(() => this.tick().catch((e) => this.logger.error('Hot tours job failed', { error: e.message })), 60000); this.tick().catch(() => {}); } stop() { clearInterval(this.timer); } async tick(now = new Date()) { const hour = Number(new Intl.DateTimeFormat('en-GB', { timeZone: 'Europe/Moscow', hour: '2-digit', hour12: false }).format(now)); const day = new Intl.DateTimeFormat('en-CA', { timeZone: 'Europe/Moscow', year: 'numeric', month: '2-digit', day: '2-digit' }).format(now); const runKey = `${day}:${hour}`; if (!this.hours.includes(hour) || this.lastRun === runKey) return; this.lastRun = runKey; await this.run(); } async run() { for (const sub of this.store.listSubscriptions()) { try { const offers = await this.tourvisor.search(sub.params); const unseen = offers.filter((o) => !sub.sentOfferIds.includes(o.id)); for (const offer of unseen) { await this.store.saveOffer(offer); const output = { text: `🔥 ${offer.hotel}\n${offer.destination}, ${offer.dates}\nЦена от ${offer.price.toLocaleString('ru-RU')} ₽`, buttons: [[{ text: 'Открыть на сайте', url: offer.siteUrl }], [{ text: 'Хочу забронировать', callback: `booking:${offer.id}` }]] }; await this.clients[sub.platform].send(sub.userId, output); } await this.store.markSubscriptionSent(sub.id, unseen.map((o) => o.id)); } catch (error) { this.logger.error('Subscription failed', { subscriptionId: sub.id, error: error.message }); } } } }

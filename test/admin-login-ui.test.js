import test from 'node:test';import assert from 'node:assert/strict';import{readFile}from'node:fs/promises';test('admin panel requires both username and password',async()=>{const html=await readFile(new URL('../web/admin/index.html',import.meta.url),'utf8');assert.match(html,/id="username"/);assert.match(html,/autocomplete="username"/);assert.match(html,/id="password"/);assert.match(html,/Неверный логин или пароль/);assert.match(html,/admin_user/);assert.match(html,/admin_pwd/)});

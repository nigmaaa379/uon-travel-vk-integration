import test from 'node:test';import assert from 'node:assert/strict';import{readFile}from'node:fs/promises';test('Tourvisor receives full desktop width so hotel tags remain visible',async()=>{const css=await readFile(new URL('../web/assets/sales-accents.css',import.meta.url),'utf8');assert.match(css,/#tourvisor-widget\{width:100%;padding:0/);assert.match(css,/#tourvisor-widget \.tv-search-form\{display:block;width:100%/);assert.match(css,/@media\(min-width:1024px\)\{\.tour-search-layout\{grid-template-columns:minmax\(0,1fr\)/);assert.match(css,/\.sales-rail\{grid-template-columns:repeat\(3,minmax\(0,1fr\)\)/)});

import test from 'node:test';import assert from 'node:assert/strict';import{readFile}from'node:fs/promises';test('provides a branded site-wide favicon at the browser default path',async()=>{const svg=await readFile(new URL('../web/favicon.svg',import.meta.url),'utf8');const fallback=await readFile(new URL('../web/favicon.ico',import.meta.url),'utf8');for(const icon of[svg,fallback]){assert.match(icon,/<svg/);assert.match(icon,/#064e63/);assert.match(icon,/#ff7a00/);assert.match(icon,/#25c4c8/)}});

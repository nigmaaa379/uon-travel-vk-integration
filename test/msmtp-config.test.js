import test from'node:test';import assert from'node:assert/strict';import{buildMsmtpConfig}from'../src/clients.js';test('msmtp config defines the default account',()=>{const text=buildMsmtpConfig({host:'smtp.mail.ru',port:465,secure:true,user:'sender@example.test',password:'secret',from:'sender@example.test'});assert.match(text,/\naccount default\n/);assert.match(text,/\nhost smtp\.mail\.ru\n/);assert.ok(text.indexOf('account default')<text.indexOf('host smtp.mail.ru'));assert.match(text,/\ntls_starttls off\n/)})

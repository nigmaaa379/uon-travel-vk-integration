import { BotAdminService } from './bot-admin.js';
export class BotAdminServiceV2 extends BotAdminService{
 clients(){const map=new Map((this.store.listBotUsers?.()||[]).map(user=>[user.key,{...user,country:String(user.profile?.destination||''),budget:String(user.profile?.budget||''),group:String(user.profile?.group||''),dates:String(user.profile?.dates||''),lastLeadId:String(user.profile?.lastLeadId||''),active:false,...this.data.clientMeta(user.key)}]));for(const sub of this.store.listSubscriptions?.()||[]){const key=`${sub.platform}:${sub.userId}`,params=sub.params||{},old=map.get(key)||{};map.set(key,{...old,key,platform:sub.platform,userId:String(sub.userId),country:String(params.destination||old.country||''),budget:String(params.budget||old.budget||''),group:String(params.group||old.group||''),dates:String(params.dates||old.dates||''),active:true,createdAt:sub.createdAt,...this.data.clientMeta(key)})}return[...map.values()]}
}

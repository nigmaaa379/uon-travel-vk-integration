import test from 'node:test';import assert from 'node:assert/strict';import{readFile}from'node:fs/promises';test('lead forms require acknowledgement of country entry information',async()=>{const app=await readFile(new URL('../web/assets/app.js',import.meta.url),'utf8');const page=await readFile(new URL('../web/legal/travel-information.html',import.meta.url),'utf8');assert.match(app,/name='countryInfoAcknowledged'|name="countryInfoAcknowledged"|checkbox\.name='countryInfoAcknowledged'/);assert.match(app,/checkbox\.required=true/);assert.match(app,/\/legal\/travel-information\.html/);assert.match(app,/countryInfoAcknowledged:data\.countryInfoAcknowledged==='on'/);assert.match(page,/правилах въезда и пребывания/);assert.match(page,/визовые, миграционные и транзитные требования/)});

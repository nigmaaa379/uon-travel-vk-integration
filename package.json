{
  "name": "uon-travel-vk-integration",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "VK chatbot qualification flow integrated with U-ON Travel leads and instant notifications",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/*.js"
  }
}

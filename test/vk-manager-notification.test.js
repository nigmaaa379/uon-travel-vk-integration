import test from'node:test';import assert from'node:assert/strict';import{Notifier}from'../src/clients.js';test('VK manager notification contains lead id but no tourist PII',async()=>{const original=global.fetch;let body;global.fetch=async(_url,options)=>{body=options.body;return new Response(JSON.stringify({response:1}),{status:200})};try{const notifier=new Notifier(null,null,{token:'token',apiVersion:'5.199',peerId:'42'},{retries:0});const result=await notifier.notify('188',{name:'Ирина',phone:'+79991234567',email:'i@example.com'});assert.equal(result.ok,true);assert.equal(body.get('peer_id'),'42');assert.match(body.get('message'),/188/);assert.doesNotMatch(body.get('message'),/Ирина|79991234567|i@example\.com/)}finally{global.fetch=original}});
